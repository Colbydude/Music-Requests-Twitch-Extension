version: '2'

services:
    musicrequestsbackend:
        volumes:
            - .:/music-requests-twitch-ext

            # necessary to prevent OS X node_modules being loaded in:
            - /music-requests-twitch-ext/backend/node_modules

        build:
            context: ./backend
            dockerfile: ./Dockerfile

        ports:
          - "8080:8080"
